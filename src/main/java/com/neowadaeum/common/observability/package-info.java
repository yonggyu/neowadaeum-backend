/**
 * 메트릭 (§12 B-48).
 *
 * <p><b>{@code common} 에 두는 이유는 여러 모듈이 같은 이름으로 세야 하기 때문이다.</b> 세이프티
 * 차단은 {@code safety} 가, 턴 결과는 {@code play} 가, 호출 비용은 {@code ai} 가 안다 — 각자
 * 자기 이름으로 세면 대시보드에서 셋을 나란히 놓을 수 없다.
 *
 * <p><b>태그에 회원 식별정보도 세션 id 도 넣지 않는다</b> (I-3). 카디널리티가 곧 비용이라는
 * 이유보다 먼저, 그것은 개인정보다.
 *
 * <p><b>원문을 넣지 않는다</b> (S-3). 태그 값은 <b>미리 정해진 짧은 목록</b>에서만 온다.
 */
package com.neowadaeum.common.observability;
