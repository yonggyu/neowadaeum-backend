/**
 * 작품 원고 (§8.1, B-51).
 *
 * <p><b>내부 패키지다.</b> {@code @NamedInterface} 를 두지 않는다 — 원고를 읽고 쓰는 것은
 * 작성자 본인의 경로뿐이며, 다른 모듈이 원고에 닿을 이유가 없다 (I-8).
 *
 * <p><b>원고 내용을 로그에 남기지 않는다</b> (S-3, R2.4). 검수 대상 원문이며 보관 주기와
 * 접근 통제가 세션 데이터와 다르다.
 */
package com.neowadaeum.authoring.draft;
