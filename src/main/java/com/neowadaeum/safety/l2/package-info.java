/**
 * L2 — AI 응답 직후 판정 (B-30).
 *
 * <p>safety 모듈의 내부 패키지다. 다른 모듈이 직접 참조하지 않는다 (§5.4).
 *
 * <p><b>S-11 — 이 패키지의 코드·주석·테스트 이름에 블록리스트 실제 항목이나 우회 표기 예시를
 * 적지 않는다.</b> 실제 문자열은 테스트 픽스처에만 둔다.
 */
package com.neowadaeum.safety.l2;
