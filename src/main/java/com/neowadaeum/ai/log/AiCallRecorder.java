package com.neowadaeum.ai.log;

/**
 * AI 호출 한 건을 남긴다 (B-25, S-3).
 *
 * <p><b>왜 어댑터가 부르는가.</b> 게이트웨이 앞단에 데코레이터로 두는 것이 이 프로젝트의 습관이고
 * (시간 제한 · 재요청 · 페이로드 검증이 전부 그렇다), 그 편이 새 어댑터가 잊을 자리를 없앤다.
 * 그러나 <b>기록해야 할 것은 와이어 원문</b>이고 그것을 아는 것은 어댑터뿐이다 — 데코레이터가
 * 보는 것은 포트 DTO 이지 실제로 나간 JSON 이 아니다.
 *
 * <p>그래서 <b>어댑터가 부르되 인터페이스로 받는다.</b> 어댑터는 무엇을 기록하는지 알지만
 * 어디에 어떻게 쓰는지는 모른다 — 비동기냐 동기냐, 어느 스토어냐는 구현의 몫이다.
 *
 * <p><b>기록 실패가 턴 생성을 깨뜨리지 않는다.</b> 구현은 예외를 밖으로 내지 않는다. 로그가
 * 본문을 막으면 <b>관측을 붙인 대가로 서비스가 멈춘다.</b>
 */
public interface AiCallRecorder {

	void record(AiCallLog.Draft draft);
}
