/**
 * FixedStoryProvider — 시나리오 파일 기반 결정론 Provider (S-3, B-44 선행)
 *
 * <p>ai 모듈의 내부 패키지다. 다른 모듈이 직접 참조하지 않는다 (§5.4).
 *
 * <p><b>{@code prod} 에서는 등록되지 않는다</b> (R3.1, I-14). 이 패키지의 구성 클래스가 그 경계다.
 */
package com.neowadaeum.ai.provider.fixed;
