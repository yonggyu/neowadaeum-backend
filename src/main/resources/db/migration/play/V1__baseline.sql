-- V1 — play 스토어 베이스라인 (§5.3)
--
-- 이 마이그레이션은 의도적으로 비어 있다. 실행되는 것 자체가 확인 대상이다.
--   · play_user 계정이 play 스키마에 테이블을 만들 수 있다 (flyway_schema_history)
--   · 이력 테이블이 스토어마다 자기 스키마에 따로 생긴다 (defaultSchema)
--
-- 도메인 테이블은 B-09 에서 추가한다.
-- 스키마 간 FK 를 만들지 않는다. 참조는 애플리케이션 레벨에서만 한다 (§5.3).
-- 시드 데이터는 여기가 아니라 B-45 다 (§2.5).
