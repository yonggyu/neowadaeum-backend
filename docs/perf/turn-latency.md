# 턴 지연 실측 (B-46)

**조건 없는 숫자는 근거가 아니다.** 아래 값이 무엇을 잰 것인지부터 적는다.

## 무엇을 쟀나

`TurnLatencyNightlyTests` 가 파이프라인 전체(`TurnPipeline.advance`)를 재고, 산출물을 둘로 남긴다.

| 산출물 | 누가 읽나 |
|---|---|
| `build/perf/turn-latency.md` | 사람. 조건과 지표가 한 장에 있다. 이 문서는 그것을 옮겨 적은 것이다 |
| `build/perf/turn-latency-run.md` | 기계. **이 회차 한 줄**이 마크다운 표의 행 모양으로 들어 있고, nightly 워크플로가 그 줄을 누적 표에 덧붙인다 (#255) |

**결정론 Provider 로 잰다.** 실 모델을 부르면 숫자가 **벤더의 그날 상태**를 재는 것이 되고 재현되지 않는다. 지연은 스텁이 인위적으로 만든다.

**측정하려는 것은 그 지연이 아니라 그 위에 우리가 얹는 몫이다.** 모델의 지연은 벤더가 정하고 우리가 줄일 수 없지만, 프롬프트 조립·L2 검수·상태 병합·저장은 우리 것이다.

**동시성은 커넥션 풀보다 크게 잡는다.** Provider 호출이 트랜잭션 안에 있었다면 그때 드러난다 — 커넥션이 지연만큼 붙들리고 뒤의 요청이 커넥션을 얻지 못한다.

## 측정 1 — 2026-08-27, GitHub Actions `ubuntu-latest`

| 조건 | 값 |
|---|---|
| Provider | 결정론 스텁 + 인위적 지연 **300 ms** |
| 동시성 | 16 |
| 표본 | 32 |
| 환경 | GitHub Actions `ubuntu-latest` · Testcontainers PostgreSQL · Redis |

| 지표 | 전체 (ms) | **Provider 를 뺀 몫 (ms)** |
|---|---|---|
| p50 | 353 | **53** |
| p95 | 371 | **71** |
| p99 | 382 | **82** |
| max | 382 | **82** |

백분위는 **보간하지 않는다** — 표본이 32건일 때 보간은 없는 정밀도를 지어낸다.

## 측정 2 — 2026-09-01, GitHub Actions `ubuntu-latest` · **nightly 최초 실행**

**이 표는 손이 아니라 nightly 가 만든 첫 값이다** (#181 해소). 조건은 측정 1 과 같다 — 결정론 스텁 + 인위적 지연 300 ms · 동시성 16 · 표본 32.

| 지표 | 전체 (ms) | **Provider 를 뺀 몫 (ms)** | 측정 1 대비 |
|---|---|---|---|
| p50 | 384 | **84** | +31 |
| p95 | 431 | **131** | +60 |
| p99 | 431 | **131** | +60 |
| max | 431 | **131** | +60 |

### 이 숫자를 회귀로 읽지 않는다

**p95 · p99 · max 가 셋 다 431 이다.** 표본이 32건이고 백분위를 보간하지 않으므로, 셋은 **같은 한 표본**을 가리킨다 — 느린 요청 하나가 세 지표를 전부 정했다. 그 하나가 빠졌다면 p95 는 측정 1 과 비슷했을 것이다.

**코드 쪽 원인도 확인했다.** 이 사이에 들어간 것 중 턴 경로를 늘린 것은 #243 뿐이다 — L2 가 화자 이름을 보게 되면서 1단 판정이 한 번 더 돈다. 그러나 블록리스트는 1분 TTL 스냅샷이며 (`PersistentBlocklistQuery`) 두 번째 호출은 **AtomicReference 읽기와 짧은 문자열 정규화**뿐이다. 60 ms 를 설명하지 못한다.

> **측정 3 이 이 자리를 닫는다.** 조건이 고정된 세 회차의 p95 폭이 18 ms 이므로 60 ms 는 표본 32건이 만든 잡음이었다. 여기서 원인을 찾지 못한 것이 맞았다 — **찾을 원인이 없었다.**

### 그래서 드러난 것 — **표본 32건으로는 p95 를 말할 수 없다**

측정 1 은 이 사실을 숨기고 있었다. 두 회차가 생기고 나서야 **p95 가 표본 하나에 흔들린다**는 것이 보인다. 지금 이 표가 답할 수 있는 것은 *"서버 몫이 100 ms 언저리"* 까지이고, **알람 임계를 이 값으로 정하면 그 알람은 러너 상태를 잰다.**

> **이슈 후보 — 표본 수를 늘린다.** 몇 건이 필요한지는 이 표가 정하지 않는다. 다만 백분위를 주장하려면 **한 표본이 지표를 결정하지 않을 만큼**은 되어야 한다.
>
> → **#255 가 됐다.** 답은 아래 "측정 방법을 고친다" 에 있다.

### 커넥션 풀은 이번에도 마르지 않았다

동시성 16 으로 32건을 돌리는 동안 커넥션을 얻지 못한 요청이 없다. 측정 1 의 결론(§9.2 — Provider 호출이 트랜잭션 밖에 있다)이 **두 번째 회차에서도 관측됐다.**

## 측정 방법을 고친다 — 표본 512건 · 회차 누적 (#255)

측정 3 부터의 조건이다. **위의 두 회차는 이 조건이 아니므로 아래 값과 나란히 놓지 않는다.**

### 왜 512 인가

백분위를 보간하지 않으므로 p95 는 정렬된 표본의 `ceil(0.95n)-1` 번째 **한 건**이다. 그 값이 흔들리지 않으려면 **그 위에 표본이 여럿 있어야** 한다.

| 표본 수 | p95 위에 남는 표본 | p99 위에 남는 표본 |
|---|---|---|
| 32 | **1** | **0** (= max) |
| 100 | 5 | 1 |
| 500 | 25 | 5 |
| **512** | **25** | **5** |

읽는 법은 이렇다 — 32건에서 p95 위에 있는 표본은 하나뿐이고 p99 는 곧 max 다. **그래서 측정 2 에서 셋이 같은 값이 됐다.** 512건이면 느린 한 건이 빠져도 p95 는 한 칸 옆으로 움직일 뿐이고, p99 와 max 가 구조적으로 갈라진다.

**p95 위에 25건, p99 위에 5건을 남기는 가장 작은 수가 둘 다 500 이다.** 512 는 거기서 측정 구조(동시성 16 × 세션당 2턴 × 세션 묶음 16)로 떨어지는 값이다.

테스트가 이 성질을 스스로 단언한다 — `above(0.99) >= 5`. 표본 수를 줄이면 그 자리에서 깨진다.

**보간은 여전히 하지 않는다.** 512건이면 보간해도 정밀도를 지어내는 것은 아니지만, 추정기를 바꾸면 회차 간 차이가 코드 변화인지 세는 법의 변화인지 구분되지 않는다.

### 표본은 세션을 늘려서 얻는다

한 세션을 계속 밀어 표본을 늘릴 수 없다. 결정론 시나리오에서 선택지 1을 계속 고르면 **다섯 번째 턴이 엔딩이고**, 끝난 세션은 더 진행되지 않는다. 그래서 세션 16개를 만들어 2턴씩 쓰고 버리는 묶음을 16번 반복한다.

**세션당 2턴은 측정 1·2 와 같다.** 첫 턴(이력 없음)과 두 번째 턴(직전 턴 있음)이 1:1 로 섞인다 — 이 비율을 바꾸면 재는 대상 자체가 달라져 회차 간 비교가 끊긴다.

### 실행 시간과의 교환

한 라운드는 동시 16건이고 라운드당 약 0.43 s(Provider 300 ms + 서버 몫)다. 32 라운드면 **약 14초**, 세션 256개 생성을 더해도 20초 안쪽이다. nightly 는 컨테이너 기동이 대부분을 차지하므로 **약 2분에서 2분 30초 언저리**가 된다. 매일 도는 잡에 한 자리 수 분이면 감당할 수 있다는 판단(#255)의 안쪽이다.

### 선언과 실제가 갈라져 있었다

측정 1·2 의 표본은 32건이지만 코드의 상수는 40 이었다. 라운드 수를 `SAMPLES / CONCURRENCY` 로 구해 **나머지 8이 조용히 잘렸다.** 이제 표본 수를 곱으로 적어 그 자리를 없앤다.

### 회차 비교 — 손으로 옮겨 적지 않는다

측정이 끝나면 그 회차 한 줄이 `build/perf/turn-latency-run.md` 로 나가고, nightly 워크플로가 **누적 표에 덧붙인다.** 표는 `area:infra` 라벨의 이슈 **지연 실측 회차 누적 (B-46)** 하나에 있으며, 워크플로가 매 회차 본문을 다시 쓴다(최근 365 회차).

- **왜 레포가 아닌가.** 워크플로에 쓰기 권한과 보호 브랜치 우회가 필요해진다. 매일 도는 잡에 그 권한을 주지 않는다.
- **왜 아티팩트가 아닌가.** 30일 뒤에 사라진다. 회차 간 비교는 그보다 오래 걸리는 판단이다.
- **열 정의는 테스트가 갖는다.** 워크플로가 아는 것은 "앞 두 줄은 머리글, 마지막 줄은 이번 회차"뿐이다.

> **2026-09-07 이전 회차의 `커밋` 열은 측정된 코드가 아니다** (#416). 테스트가 `GITHUB_SHA` 를 적었는데 `schedule` 에서 그 값은 **기본 브랜치(`main`)의 SHA** 이고, nightly 는 `ref: backend` 를 체크아웃한다. 세 브랜치의 트리가 같은 동안은 내용이 같아 드러나지 않았고, `backend` 가 앞선 2026-09-06 회차에서 처음 어긋났다 — 그 행은 `94718d5`(main)를 적었지만 잰 것은 `85cb635`(backend)다.
>
> **쌓인 행은 되쓰지 못한다** — nightly 가 매 회차 원장 본문을 통째로 다시 쓴다. 이 문단이 그 사실을 드는 유일한 자리다. 측정 1~3 의 **숫자는 영향받지 않는다**: 조건이 같은 트리에서 잰 값이고, 틀린 것은 그 코드를 *가리키는 이름*이었다.

이 문서는 그중 **판단이 달라진 회차만** 옮겨 적는다. 매일의 숫자를 전부 옮기면 표가 로그가 되고, 로그는 아무도 읽지 않는다.

### 러너 변동은 열로 만들지 않는다

**결론: 만들지 않는다.** 대신 조건을 행마다 싣는다.

- **회차 안의 변동**은 이미 p50 과 p95 의 폭이 말한다. 같은 것을 세는 열을 하나 더 두면 둘이 갈라지는 날이 온다.
- **회차 사이의 변동**은 누적 표의 같은 열을 세로로 읽으면 나온다 — 조건이 고정된 회차가 쌓이면 **그 폭 자체가 하나의 값이 된다.** 표준편차를 회차마다 계산해 넣어도 그 값은 회차 하나 안의 것이라 회차 간 변동을 말하지 못한다.
- **진짜 위험은 조건이 다른 회차를 같은 열에서 비교하는 것이다.** 그래서 표본 수 · 동시성 · Provider 지연 · 커밋 · 실행을 행마다 함께 싣는다. 조건이 바뀐 회차는 표에서 눈으로 걸러진다.

## 측정 3 — 2026-09-03 ~ 09-05, 표본 512 회차 셋

**512건 조건의 첫 회차들이다.** 누적 표(#345)에서 옮겨 적었다 — 조건이 셋 다 같아 같은 열에서 비교한다.

| 회차 (UTC) | 커밋 | 서버 p50 | 서버 p95 | 서버 p99 | 서버 max |
|---|---|---|---|---|---|
| 2026-09-03T20:30 | `eb04409` | 51 | 87 | 127 | 150 |
| 2026-09-04T20:15 | `093b86b` | 36 | 69 | 101 | 106 |
| 2026-09-05T19:54 | `56a9bef` | 43 | 79 | 100 | 119 |
| **폭** | | **15** | **18** | **27** | **44** |

조건은 셋 다 표본 **512** · 동시성 **16** · Provider 인위 지연 **300 ms**. 전체 지연은 각 값에 300 을 더한 것이다.

### 표본을 늘린 것이 실제로 작동했다

**p99 와 max 가 갈라졌다.** 측정 2 에서는 p95 · p99 · max 가 셋 다 431 이었다 — 한 표본이 세 지표를 전부 정했다. 세 회차 모두에서 넷이 서로 다른 값이다.

이것이 512 를 고른 이유 그대로다 (위 "왜 512 인가"). **테스트가 단언하는 `above(0.99) >= 5` 가 문서의 주장이 아니라 관측으로 확인됐다.**

### 측정 2 의 `+60` 은 회귀가 아니었다

측정 2 는 측정 1 보다 p95 가 60 ms 높았고, 그 문서는 그것을 회귀로 읽지 않기로 하면서 **"코드 쪽 원인을 찾지 못했다"** 로 남겨 두었다.

**이제 답이 나온다.** 조건이 고정된 세 회차의 p95 폭이 **18 ms** 다. 회차 간 변동이 그 정도인 측정에서 60 ms 차이는 **표본 32건이 만든 잡음**이지 코드 변화가 아니다. 측정 1 과 2 사이에 #243 이 턴 경로를 늘린 것은 사실이지만, 그것이 60 ms 를 설명하지 못한다는 그때의 판단이 맞았다.

**남는 교훈은 숫자가 아니라 절차다** — 표본이 모자란 측정은 *틀린 값*을 주지 않고 **틀린 이야기를 준다.** 두 회차 동안 우리는 있지도 않은 회귀의 원인을 찾고 있었다.

### 커넥션 풀은 512건에서도 마르지 않았다

동시성 16 으로 회차당 512건을 돌리는 동안 커넥션을 얻지 못한 요청이 없다. 표본이 16배로 늘어도 그대로다 — Provider 호출이 트랜잭션 밖에 있다는 것(§9.2)이 세 번째 조건에서도 관측됐다.

## 읽는 법

**커넥션 풀은 마르지 않았다.** Provider 호출이 트랜잭션 밖에 있다는 것(§9.2)이 설계가 아니라 **관측된 사실**이다 — 측정 1 · 2 · 3 전부에서.

**우리 몫은 p95 에서 69~87 ms 다.** 하나의 값이 아니라 폭으로 적는 것이 이 표가 말할 수 있는 전부다 — 세 회차가 69 · 79 · 87 이고, 그 차이는 코드가 아니라 러너가 만든다.

**p50 과 p99 의 폭은 회차마다 57 · 65 · 76 ms 다.** 측정 1·2 가 "30 ms 이내"로 읽었던 것은 표본 32건에서 p99 가 max 와 같았기 때문이고, **실제 꼬리는 그보다 길다.** 부하가 걸리면 서버 쪽 지연도 벌어진다 — 다만 그 절대값이 100 ms 대라 모델 대기 앞에서 작다.

**이 숫자가 작다는 것이 이 측정의 결론이 아니다.** 결론은 **전체 지연의 거의 전부가 모델 대기라는 것**이다.

## 스트리밍 도입 판단

**지금은 도입하지 않는다.** 측정 3 이 이 판단을 바꾸지 않았고, 근거를 표본 512 로 갈아 끼웠다.

사용자가 기다리는 시간의 대부분은 모델이 첫 토큰을 내놓기까지의 시간이며, 그 앞뒤로 우리가 얹는 것은 p95 에서 **69~87 ms** 다. **서버를 더 깎아도 체감은 거의 바뀌지 않는다.**

스트리밍이 바꾸는 것은 총 시간이 아니라 **첫 글자가 보이는 시점**이다. 그것이 의미 있으려면 실 모델의 지연이 얼마인지 알아야 하고, 지금은 그 값이 없다 — 결정론 Provider 로만 쟀기 때문이다. **표본을 512 로 늘려도 이 공백은 메워지지 않는다.** 표본 수는 우리 몫을 정밀하게 만들 뿐, 재지 않은 것을 재 주지 않는다.

**다시 볼 시점**: 실 Provider 로 운영 트래픽이 흐른 뒤, B-48 의 `ai.call.latency` 가 실제 모델 지연을 보여 줄 때. 그때 `play.turn.duration` 과의 차이가 여전히 100 ms 대라면, 개선의 여지는 서버가 아니라 **화면**에 있다.

그리고 스트리밍은 공짜가 아니다 — **L2 검수는 본문이 다 나온 뒤에 판정한다** (I-2). 토큰을 흘려보내면서 검수하려면 "이미 사용자에게 도달한 문장을 되돌리는" 문제가 생기고, 그것은 15세 등급 서비스에서 성능보다 무거운 문제다.

> **B-46 의 DoD 는 이 절이 든다.** `docs/tasks.md` 가 *"p95 실측치를 `docs/perf/` 에 기록 → 스트리밍 도입 판단 근거"* 라고 적은 그 판단이다.

## B-48 알람 임계값의 출발점

**아직 정하지 않는다. 그러나 채우는 조건 둘 중 하나가 채워졌다.**

| 신호 | 출발점 | 상태 |
|---|---|---|
| `play.turn.duration` p95 | **실 Provider 지연 + (서버 몫 p99 의 두 배)** | **보류.** 서버 몫 p99 는 이제 잰다 — 실 Provider 지연이 없다 |
| 턴 예산 소진 | 25 s 예산의 **80% 도달 비율** | 유효. 실측이 아니라 예산 정의에서 나온 값이다 |

1. ~~512건 조건의 회차가 여러 번 쌓여 **서버 몫 p99 의 회차 간 변동 폭**이 보일 것~~ — **충족.** 세 회차의 p99 가 127 · 101 · 100 이고 폭은 27 ms 다. 한 회차의 p99 로 임계를 정하면 그 27 ms 안에서 알람이 켜졌다 꺼진다.
2. **실 Provider 지연을 알 것** — B-48 의 `ai.call.latency` 가 그것을 준다. **미충족.** 그전까지 절대값을 적지 않는다.

**둘째가 남아 있는 한 임계값은 여전히 비어 있다.** 서버 몫만으로 임계를 정하면 그 알람은 전체 지연의 소수 지분을 감시하게 된다 — 사용자가 겪는 것은 그 합이다.

## 다음 측정

nightly 가 매일 18:00 UTC 에 돌며 `build/perf/` 를 아티팩트로 남기고, 회차 한 줄을 누적 이슈(#345)에 덧붙인다. **이 문서는 그중 판단이 달라진 회차만 옮겨 적는다.**

**측정 3 이 그런 회차였다** — 표본이 바뀌어 백분위를 처음으로 말할 수 있게 됐고, 측정 2 의 `+60` 이 잡음이었다는 것이 드러났다.

**다음에 이 문서를 고치는 조건**은 셋이다.

- 조건(표본 · 동시성 · Provider 지연)이 **바뀐** 회차가 나온다
- 서버 몫이 위 폭(p95 69~87)을 **벗어난** 회차가 이어진다 — 한 회차는 러너이고, 이어지면 코드다
- **실 Provider 지연이 생긴다** (B-48). 그때 스트리밍 판단과 알람 임계를 함께 다시 연다
