# ─────────────────────────────────────────────────────────────
# B-04 DoD 검증용 합성 자격 증명. 머지하지 않는다.
#
# 아래 값은 어떤 서비스의 자격 증명도 아니다. 형식을 흉내 내지도 않았다.
# 무작위 문자열이며 실재하는 계정·키·토큰과 무관하다.
#
# 값 안에 FAKE / DO-NOT-USE 같은 단어를 넣을 수 없다 — gitleaks 가
# 스톱워드 허용목록으로 건너뛰어 탐지 자체가 일어나지 않는다.
# 그래서 가짜임은 파일명과 변수명으로 선언한다.
#
# 확인 후 PR 을 닫고 이 브랜치를 삭제한다 (§7.1-4).
# ─────────────────────────────────────────────────────────────
NEOWADAEUM_GITLEAKS_VERIFICATION_TOKEN = "b7Qx2Lm9Vt4Rf8Kd1Zp6Ns3Wj5Hy0Ug"
